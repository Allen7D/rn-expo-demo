import { Text, View, Button, Image, StyleSheet, Platform } from 'react-native';
import { Link, router } from 'expo-router';

export default function ReadDetailScreen() {
  return (
    <View>
      <Text>ReadDetail</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});
